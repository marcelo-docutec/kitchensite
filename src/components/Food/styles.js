import { styled } from '@material-ui/core';
import { Card as CardBase } from '@material-ui/core';

const Styled = styled(CardBase)({
  overflow: 'visible',
  marginBottom: '15px',
  borderRadius: 0,
});

export default { Styled };
