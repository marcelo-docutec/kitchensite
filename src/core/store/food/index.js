import * as types from './types';
import * as actions from './actions';
import * as reducer from './reducer';

/**
 * Actions
 * getByName(name: string): return all food with that name
 * getAll(): return all foods
 */

export { types, actions, reducer };
